# Render configuration for Basketball AI API
# https://render.com

# Python version
python-3.11.0

# Build command (will be set in render.yaml)
# pip install -r requirements.txt

# Start command
# uvicorn main:app --host 0.0.0.0 --port $PORT
