{
  "//": "Marks the pose-tracking signal-processing module as pure ES modules so it runs unchanged under Node's test runner (`npm run test:pose`). Metro/Babel ignore the `type` field and transform these files normally; all imports here are explicit relative file paths, so no package resolution is affected. No React/React Native imports live here — this is pure rep-counting math.",
  "type": "module"
}
